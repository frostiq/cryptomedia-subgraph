type NFT @entity {
  id: ID!
  contract: NFTContract!
  tokenID: BigInt!
  owner: Bytes!
  creatorName: String!
  tokenURI: String!
}

type NFTContract @entity {
  id: ID!
  name: String!
  symbol: String
  platform: Platform!
}

enum Platform {
  NiftyGateway
}